version: '3.8'

services: 
  redis:
    image: redis:latest
    restart: always
    ports: 
      - 6379:6379
    command: redis-server --requirepass avtushko
  api:
    build: ./tellme-server
    ports:
      - 3000:3000
  client:
    build: ./tellme
    ports:
      - 3030:3030